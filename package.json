{
  "name": "daily-deutsch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily German drill — vocabulary, grammar tables and spaced repetition, with progress that follows you.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node test/drill.mjs && node test/repository.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "esbuild": "^0.21.5",
    "jsdom": "^30.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=20"
  }
}
